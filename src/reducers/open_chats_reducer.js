
const initialState = [
    {
        name: 'Global Chat',
    }
];

export default function openChats(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}
